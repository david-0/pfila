{
  "name": "pfila2019-starter",
  "version": "1.0.0",
  "description": "Starter for Homepage 2019",
  "repository": {
    "type": "git",
    "url": "https://github.com/david-0/pfila.git"
  },
  "scripts": {
    "start": "gulp",
    "install": "npm run install-clean; npm run install-server; npm run install-client",
    "install-clean": "rm -rf dist",
    "install-server": "cd server; rm -rf dist; npm run build; cp -pPr dist ../dist; cd ..;",
    "install-client": "cd client; npm run build-prod; cp -pPr dist ../dist; cd ..;",
    "test": "gulp test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gulp": "^4.0.0"
  },
  "devDependencies": {}
}
